# IDL Generation Summary

## Status: ✅ Complete

The IDL (Interface Definition Language) for the `cryptoscore_factory` Solana program has been successfully generated and is properly configured across all necessary locations.

## IDL File Locations

### 1. Build Output (Source of Truth)
- **Path**: `solana/target/idl/cryptoscore_factory.json`
- **Generated by**: Anchor build process
- **Size**: 267 lines
- **Checksum**: `91db14315db974e8a3d41b8bdbc409cb`

### 2. Frontend Integration
- **Path**: `solana/app/src/idl/cryptoscore_factory.json`
- **Purpose**: Used by React frontend for program interaction
- **Size**: 267 lines
- **Checksum**: `91db14315db974e8a3d41b8bdbc409cb`
- **Status**: ✅ Identical to source

### 3. TypeScript Types
- **Path**: `solana/target/types/cryptoscore_factory.ts`
- **Generated by**: Anchor build process
- **Purpose**: TypeScript type definitions for the program

## IDL Structure

The generated IDL includes:

### Instructions (3)
1. **initializeFactory** - Initialize the factory with authority and platform fee
   - Accounts: factory, authority, systemProgram
   - Args: platformFeeBps (u16)

2. **createMarket** - Create a new prediction market
   - Accounts: factory, marketRegistry, marketAccount, creator, systemProgram
   - Args: matchId (string), entryFee (u64), kickoffTime (i64), endTime (i64), isPublic (bool)

3. **getMarkets** - Get paginated list of markets with filtering
   - Accounts: factory
   - Args: filterCreator (Option<Pubkey>), filterPublic (Option<bool>), page (u32), pageSize (u32)

### Accounts (2)
1. **Factory**
   - authority: Pubkey
   - marketCount: u64
   - platformFeeBps: u16
   - bump: u8

2. **MarketRegistry**
   - factory: Pubkey
   - marketAddress: Pubkey
   - creator: Pubkey
   - matchId: string
   - createdAt: i64
   - isPublic: bool
   - entryFee: u64
   - kickoffTime: i64
   - endTime: i64
   - bump: u8

### Events (1)
- **MarketCreated** - Emitted when a new market is created
  - Indexed: market (Pubkey), creator (Pubkey)
  - Data: matchId (string), entryFee (u64), kickoffTime (i64), isPublic (bool)

### Errors (7)
- InvalidPlatformFee (6000)
- InvalidMatchId (6001)
- MatchIdTooLong (6002)
- ZeroEntryFee (6003)
- InvalidKickoffTime (6004)
- InvalidEndTime (6005)
- MarketCountOverflow (6006)

## Frontend Integration

The IDL is imported in the frontend at:
- **File**: `solana/app/src/config/programs.ts`
- **Import**: `export { default as FactoryIDL } from "../idl/cryptoscore_factory.json"`

This allows the frontend to:
1. Create typed program instances with Anchor
2. Call program instructions with proper type checking
3. Decode program accounts and events
4. Handle program errors gracefully

## Verification

```bash
# Verify IDL files are identical
md5sum target/idl/cryptoscore_factory.json app/src/idl/cryptoscore_factory.json
# Output: 91db14315db974e8a3d41b8bdbc409cb (both files)

# Check line count
wc -l target/idl/cryptoscore_factory.json app/src/idl/cryptoscore_factory.json
# Output: 267 lines each
```

## Program ID

The program is deployed with the following IDs:

- **Localnet**: `93CjfuYYswDbcjasA1PTUmHhsqFsBQC4JnsiKB8nKJhP`
- **Devnet**: `5zADKCecxATSEsCuH5MJa1JdfXGeBLNwEYnkCbqdaYmZ`
- **Testnet**: `TestnetFactoryId1111111111111111111111111`
- **Mainnet**: `MainnetFactoryId111111111111111111111111`

## Maintenance

### Regenerating IDL

If you make changes to the smart contract, regenerate the IDL:

```bash
# Option 1: Build only (generates IDL in target/idl/)
cd solana
anchor build

# Option 2: Build and export to frontend (requires @types/node)
npm run idl:build

# Option 3: Export existing IDL to frontend
npm run idl:export

# Option 4: Manual copy (if scripts fail)
cp target/idl/cryptoscore_factory.json app/src/idl/cryptoscore_factory.json
```

### Verification Commands

```bash
# List available IDLs
npm run idl:list

# Check if IDLs are in sync
diff target/idl/cryptoscore_factory.json app/src/idl/cryptoscore_factory.json
```

## Notes

1. The IDL is automatically generated by Anchor during the build process
2. The frontend IDL must be kept in sync with the build output
3. TypeScript types are generated alongside the IDL
4. The IDL export script (`scripts/export-idls.ts`) automates the sync process
5. All IDL files are tracked in git for version control

## Related Files

- Smart Contract: `solana/programs/factory/src/lib.rs`
- IDL Export Script: `solana/scripts/export-idls.ts`
- Frontend Config: `solana/app/src/config/programs.ts`
- Anchor Config: `solana/Anchor.toml`

---

**Generated**: 2024-11-28  
**Status**: Production Ready ✅
